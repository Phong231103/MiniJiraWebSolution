namespace Web.Domain.Enums
{
    public enum StatusName
    {
        NOT_FOUND = 404,
        VALIDATION_ERROR = 400,
        UNAUTHORIZED = 401,
        FORBIDDEN = 403,
        REQUIRED = 400
    }
}
